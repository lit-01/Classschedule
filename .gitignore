# 构建产物：build/ 里有 windeployqt 拷进来的 Qt 运行库和 exe，
# 好几十 MB，而且都是能重新生成的，别进仓库。
build/

# 安卓交叉编译产物（qt-cmake + androiddeployqt 在根目录生成的整套
# android-build / .so / gradle / CMakeCache，几十 MB，可重新生成）
build-android/

# 截图是调试跑出来的，每次都变，进仓库只会让仓库越来越臃肿
shots/

# Qt Creator / clangd / VS 的本地配置
CMakeLists.txt.user*
.qtc_clangd/
.vscode/
.cache/

# 系统垃圾
Thumbs.db
.DS_Store
